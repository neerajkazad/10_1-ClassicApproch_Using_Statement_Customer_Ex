package com.ca.notes;

public class Notes {
	/**
	 * In this example we are using Statement. So when we use static sql query
	 * then Creator phase is not required that's the reason we directly written
	 * GetAllCustomerStatementCallback and we pass the GetAllCustomersStatementCallback object 
	 * to the jdbcTemplate.
	 * 
	 * Here we don't required GetAllCustomerStatementCreator 
	 */
}
